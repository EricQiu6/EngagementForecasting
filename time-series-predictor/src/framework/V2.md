running comparison script in V1:

```
cd time-series-predictor
source venv/bin/activate && python model/train_evaluate.py
```

running comparison script in V2:

```
cd time-series-predictor
source venv/bin/activate && python demo_framework_v2.py
```
